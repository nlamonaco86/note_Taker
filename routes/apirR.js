//dependencies
const path = require("path");
const fs = require("fs");
let noteDB = require("../db/db.json");

// module.exports = function (app) {

//     // need to see the notes DB JSON

//     // post new notes to the DB when they save it

//     // read from the notes DB when they load it

//     // overwrite the notes DB to blank when they delete 

// };




